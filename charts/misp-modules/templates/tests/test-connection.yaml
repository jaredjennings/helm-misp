apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "misp-modules.fullname" . }}-test-connection"
  labels:
    {{- include "misp-modules.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "misp-modules.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
